{
  "scripts": {
    "zip:axios": "rm -rf node_modules && npm install axios && mkdir nodejs && cp -r node_modules nodejs && zip -r axios.zip nodejs && rm -rf nodejs",
    "zip:winston": "rm -rf node_modules && npm install winston && mkdir nodejs && cp -r node_modules nodejs && zip -r winston.zip nodejs && rm -rf nodejs"
  },
  "dependencies": {
    "axios": "^0.26.0",
    "winston": "^3.6.0"
  }
}
